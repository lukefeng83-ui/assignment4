[project]
name = "assignment4-cifar10-diffusion-ebm-api"
version = "0.1.0"
description = "CIFAR-10 diffusion and energy-based model FastAPI assignment"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "torch",
    "torchvision",
    "matplotlib",
    "pillow",
]
